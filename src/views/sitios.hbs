<div class="card">
    <div class="card-header">
        <h1>Agregar sitios</h1>
    </div>
    <div class="card-body">
        <form action="/sitios/add" method="POST">
            <labell for="cuadrilla">Ingrese sitio</labell>
            <input type="text" name="sitio" class="form-control" value="{{sitio.sitio}}" placeholder="sitio">
            <button>Guardar</button>
        </form>
    </div>
</div>
<div class="card">
    <div class="card-header">
        <h1>Sitios creados</h1>
    </div>
    <div class="card-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>N°</th>
                    <th>Sitios</th>
                    {{!-- <th></th> --}}
                </tr>
            </thead>
            <tbody>
               {{#each sitios}}
               <tr>
                <td>{{@index}}</td>
                <td>{{sitio}}</td>
                {{!-- <td><a href="/edit-cuadrilla/{{_id}}">Editar</a></td> --}}
               </tr>
               {{/each}}
            </tbody>
        </table>
    </div>
</div>